# Copy to backend/.env for local overrides. Never commit .env.
# Auth is off by default so the Next.js dashboard can reach the API without a login flow.
MARSAR_FORENSIC_SECRET_KEY=replace-with-a-long-random-offline-secret
MARSAR_REQUIRE_AUTH=false
MARSAR_AUTH_TOKEN_TTL_SECONDS=28800
