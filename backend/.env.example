# Copy to a local .env file or set through your deployment environment. Never commit .env.
MARSAR_FORENSIC_SECRET_KEY=replace-with-a-long-random-offline-secret
MARSAR_REQUIRE_AUTH=true
MARSAR_AUTH_TOKEN_TTL_SECONDS=28800
